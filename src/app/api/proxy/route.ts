export async function GET() {
  return new Response('API proxy to FastAPI backend will be implemented in Task #5', {
    status: 200,
    headers: { 'content-type': 'text/plain' },
  })
}

export async function POST() {
  return new Response('API proxy to FastAPI backend will be implemented in Task #5', {
    status: 200,
    headers: { 'content-type': 'text/plain' },
  })
}